apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deployment
  labels:
    app_env_stage: dev
spec:
  replicas: 6
  selector:
    matchLabels:
      app_env_stage: dev
  template:
    metadata:
      labels:
        app_env_stage: dev
    spec:
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379